# 셸 스크립트는 LF 로 고정(Windows 에서 CRLF 로 바뀌면 실행 깨짐)
*.sh text eol=lf

# C# 소스는 Windows 표준 CRLF
*.cs text eol=crlf
*.csproj text eol=crlf

# 바이너리(자동 판별에 맡기지 않음)
*.ico binary
*.traineddata binary
*.dll binary
